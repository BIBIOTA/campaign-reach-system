package com.example.campaignreach.campaign.domain;

/**
 * Placeholder marker class for the campaign domain package.
 *
 * <p>Exists so the module boundary is concrete and the ArchUnit guard has a
 * class to scan before real domain types are introduced in later tasks.
 * Replace/remove once the campaign domain model is implemented.
 */
public final class CampaignModuleMarker {

    private CampaignModuleMarker() {}
}
