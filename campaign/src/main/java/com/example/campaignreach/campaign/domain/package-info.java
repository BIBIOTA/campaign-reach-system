/**
 * Campaign module — domain layer (entities, aggregates, repositories, domain services).
 *
 * <p>Placeholder package establishing the module boundary; populated by later tasks.
 * This package MUST NOT be imported by the reach module (enforced by ArchUnit).
 */
package com.example.campaignreach.campaign.domain;
