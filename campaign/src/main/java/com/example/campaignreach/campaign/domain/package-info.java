/**
 * Campaign module — domain layer (entities, aggregates, repositories, domain services).
 *
 * <p>Holds the {@code Campaign} aggregate root, its lifecycle/type enums, the JPA
 * repository port and audit wiring (task 3.1). This package MUST NOT be imported by
 * the reach module (enforced by ArchUnit).
 */
package com.example.campaignreach.campaign.domain;
