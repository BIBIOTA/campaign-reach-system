# CREATE INDEX CONCURRENTLY cannot run inside a transaction block, so this migration must execute
# outside Flyway's default per-migration transaction (task 11.1, NFR-005). See the .sql header for the
# rationale and the recovery procedure if a CONCURRENTLY build fails (it leaves an INVALID index).
executeInTransaction=false
