/**
 * Shared kernel — cross-module event schema (Kafka message contracts).
 *
 * <p>This is the ONLY channel through which campaign and reach communicate.
 * Placeholder package establishing the module boundary; populated by later tasks.
 */
package com.example.campaignreach.shared.event;
