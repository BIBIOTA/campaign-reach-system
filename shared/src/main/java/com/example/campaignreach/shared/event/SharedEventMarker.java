package com.example.campaignreach.shared.event;

/**
 * Placeholder marker for the shared event package — the only cross-module
 * communication channel between campaign and reach.
 *
 * <p>Replace/remove once real event schema types are introduced in later tasks.
 */
public final class SharedEventMarker {

    private SharedEventMarker() {
    }
}
