package com.example.campaignreach.reach.orchestrator;

/**
 * Placeholder marker class for the reach orchestrator package.
 *
 * <p>Exists so the module boundary is concrete and the ArchUnit guard has a
 * class to scan before real reach types are introduced in later tasks.
 * Replace/remove once the reach orchestration logic is implemented.
 */
public final class ReachModuleMarker {

    private ReachModuleMarker() {
    }
}
